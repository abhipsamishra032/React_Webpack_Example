
import React, {Component} from 'react';
import ReactDOM from 'react-dom';

// Create a root element
export default class App extends Component {
    render() {
        return(
            <div className="root">React-webpack application</div>
        );
    }
}
